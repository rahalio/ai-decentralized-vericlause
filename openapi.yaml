# Deprecated monolith.
# Canonical OpenAPI lives under packages/openapi-core/src/ — one YAML file per domain
openapi: 3.1.0
info:
  title: Vericlause (moved)
  version: 0.1.0
  description: See packages/openapi-core/src/{domain}.yaml
paths: {}
