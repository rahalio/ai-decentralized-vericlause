/**
 * ExecuteGetSpecification - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: specifications
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetSpecification Use Case
 *
 * DDD: Application use case for get Specification.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetSpecificationInput, GetSpecificationOutput } from "../dto/specification.dto";
import type { ExecutionContextService, IdGeneratorService } from "@vericlause/services/_shared/index.js";
import type { SpecificationRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetSpecification {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly specification: SpecificationRepository
  ) {}

  async execute(input: GetSpecificationInput): Promise<GetSpecificationOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.spcId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).specId) {
      throw new ValidationError("specId is required");
    }
    const specId = (input as any).specId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.specification.getSpecification({ specId: specId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${specId}`);
    }

    return entity as GetSpecificationOutput;
  }
}
