/**
 * ExecuteGetReleaseGate - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: release-gates
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetReleaseGate Use Case
 *
 * DDD: Application use case for get ReleaseGate.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetReleaseGateInput, GetReleaseGateOutput } from "../dto/release-gate.dto";
import type { ExecutionContextService, IdGeneratorService } from "@vericlause/services/_shared/index.js";
import type { ReleaseGateRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetReleaseGate {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly releaseGate: ReleaseGateRepository
  ) {}

  async execute(input: GetReleaseGateInput): Promise<GetReleaseGateOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.gatId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).gateId) {
      throw new ValidationError("gateId is required");
    }
    const gateId = (input as any).gateId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.releaseGate.getReleaseGate({ gateId: gateId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${gateId}`);
    }

    return entity as GetReleaseGateOutput;
  }
}
