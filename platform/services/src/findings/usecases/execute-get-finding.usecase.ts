/**
 * ExecuteGetFinding - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: findings
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetFinding Use Case
 *
 * DDD: Application use case for get Finding.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetFindingInput, GetFindingOutput } from "../dto/finding.dto";
import type { ExecutionContextService, IdGeneratorService } from "@vericlause/services/_shared/index.js";
import type { FindingRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetFinding {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly finding: FindingRepository
  ) {}

  async execute(input: GetFindingInput): Promise<GetFindingOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.fndId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).findingId) {
      throw new ValidationError("findingId is required");
    }
    const findingId = (input as any).findingId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.finding.getFinding({ findingId: findingId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${findingId}`);
    }

    return entity as GetFindingOutput;
  }
}
