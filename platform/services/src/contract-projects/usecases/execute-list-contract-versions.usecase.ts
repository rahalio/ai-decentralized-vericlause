/**
 * ExecuteListContractVersions - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: contract-projects
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteListContractVersions Use Case
 *
 * DDD: Application use case for list Version.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ListContractVersionsInput, ListContractVersionsOutput } from "../dto/version.dto";
import type { ExecutionContextService, IdGeneratorService } from "@vericlause/services/_shared/index.js";
import type { VersionRepository } from "../ports";

export class ExecuteListContractVersions {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly version: VersionRepository
  ) {}

  async execute(input: ListContractVersionsInput): Promise<ListContractVersionsOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.prjId();

    // All field names extracted from OpenAPI spec: none
    // List entities via repository
    const result = await this.version.listContractVersions({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return result as ListContractVersionsOutput;
  }
}
