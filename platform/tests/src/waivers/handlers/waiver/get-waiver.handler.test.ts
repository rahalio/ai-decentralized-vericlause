/**
 * GetWaiver handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: waivers
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getWaiver } from "@vericlause/api-server/domains/waivers/handlers";
import type { WaiversDDDDependencies } from "@vericlause/api-server/domains/waivers";
import { createWaiver as createWaiverEntity } from "@vericlause/factories/waivers";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@vericlause/tests/shared";
import { WAIVERS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetWaiver", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: WaiversDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<WaiversDDDDependencies>(
      WAIVERS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createWaiverEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetWaiver, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { waiverId: entity.id };

    await getWaiver(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetWaiver.execute).toHaveBeenCalledWith(expect.objectContaining({ waiverId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { waiverId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetWaiver, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getWaiver(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
