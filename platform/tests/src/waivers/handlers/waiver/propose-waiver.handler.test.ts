/**
 * ProposeWaiver handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: waivers
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { proposeWaiver } from "@vericlause/api-server/domains/waivers/handlers";
import type { WaiversDDDDependencies } from "@vericlause/api-server/domains/waivers";
import { createWaiver as createWaiverEntity } from "@vericlause/factories/waivers";
import { createCreateWaiverRequest } from "@vericlause/factories/waivers";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@vericlause/tests/shared";
import { WAIVERS_USE_CASES } from "../__factories__/use-cases.js";

describe("ProposeWaiver", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: WaiversDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<WaiversDDDDependencies>(
      WAIVERS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = createCreateWaiverRequest();
    const entity = createWaiverEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeProposeWaiver, "execute").mockResolvedValue(useCaseResult);

    await proposeWaiver(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeProposeWaiver.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(201);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
