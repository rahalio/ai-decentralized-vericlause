/**
 * Waiver Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: waivers
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { WaiverEntity } from "@vericlause/core/waivers/models/index.js";
import { waiversSchemas } from "@vericlause/core/waivers";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "wvr";

export function createWaiver(overrides: Partial<WaiverEntity> = {}) {
  const raw = {
    id: overrides.id ?? `${DOMAIN_PREFIX}_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    approverCount: overrides.approverCount ?? faker.number.int({ min: 1, max: 100 }),
    expiresAt: overrides.expiresAt ?? new Date().toISOString(),
    findingId: overrides.findingId ?? `fnd_${ulid().toLowerCase()}`,
    justification: overrides.justification ?? faker.lorem.word(),
    requiredApprovals: overrides.requiredApprovals ?? faker.number.int(),
    status: overrides.status ?? "proposed",
    waiverId: overrides.waiverId ?? `wvr_${ulid().toLowerCase()}`,
    ...overrides,
  };

  return waiversSchemas.Waiver.parse(raw);
}

export function waiverBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createWaiver(overrides));
}
