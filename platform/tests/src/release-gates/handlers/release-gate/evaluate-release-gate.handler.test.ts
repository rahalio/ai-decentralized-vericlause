/**
 * EvaluateReleaseGate handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: release-gates
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { evaluateReleaseGate } from "@vericlause/api-server/domains/release-gates/handlers";
import type { ReleaseGatesDDDDependencies } from "@vericlause/api-server/domains/release-gates";
import { createReleaseGate as createReleaseGateEntity } from "@vericlause/factories/release-gates";
import { createCreateReleaseGateRequest } from "@vericlause/factories/release-gates";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@vericlause/tests/shared";
import { RELEASEGATES_USE_CASES } from "../__factories__/use-cases.js";

describe("EvaluateReleaseGate", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ReleaseGatesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ReleaseGatesDDDDependencies>(
      RELEASEGATES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = createCreateReleaseGateRequest();
    const entity = createReleaseGateEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeEvaluateReleaseGate, "execute").mockResolvedValue(useCaseResult);

    await evaluateReleaseGate(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeEvaluateReleaseGate.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(201);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
