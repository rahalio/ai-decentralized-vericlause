/**
 * GetAnalysisJob handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: analysis-jobs
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getAnalysisJob } from "@vericlause/api-server/domains/analysis-jobs/handlers";
import type { AnalysisJobsDDDDependencies } from "@vericlause/api-server/domains/analysis-jobs";
import { createAnalysisJob as createAnalysisJobEntity } from "@vericlause/factories/analysis-jobs";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@vericlause/tests/shared";
import { ANALYSISJOBS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetAnalysisJob", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: AnalysisJobsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<AnalysisJobsDDDDependencies>(
      ANALYSISJOBS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createAnalysisJobEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetAnalysisJob, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { jobId: entity.id };

    await getAnalysisJob(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetAnalysisJob.execute).toHaveBeenCalledWith(expect.objectContaining({ jobId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { jobId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetAnalysisJob, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getAnalysisJob(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
