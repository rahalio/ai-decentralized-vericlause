/**
 * ListAnalysisJobs handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: analysis-jobs
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { listAnalysisJobs } from "@vericlause/api-server/domains/analysis-jobs/handlers";
import type { AnalysisJobsDDDDependencies } from "@vericlause/api-server/domains/analysis-jobs";
import { createAnalysisJob as createAnalysisJobEntity } from "@vericlause/factories/analysis-jobs";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@vericlause/tests/shared";
import { ANALYSISJOBS_USE_CASES } from "../__factories__/use-cases.js";

describe("ListAnalysisJobs", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: AnalysisJobsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<AnalysisJobsDDDDependencies>(
      ANALYSISJOBS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with list of entities", async () => {
    const entities = [
      createAnalysisJobEntity({ orgId }),
      createAnalysisJobEntity({ orgId }),
    ];
    const useCaseResult = {
      data: { items: entities, nextCursor: undefined, prevCursor: undefined },
      meta: { correlationId: "test", timestamp: new Date().toISOString(), pagination: {} }
    };
    
    vi.spyOn(mockDeps.executeListAnalysisJobs, "execute").mockResolvedValue(useCaseResult);

    await listAnalysisJobs(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeListAnalysisJobs.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
