/**
 * Specification Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: specifications
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { SpecificationEntity } from "@vericlause/core/specifications/models/index.js";
import { specificationsSchemas } from "@vericlause/core/specifications";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "spc";

export function createSpecification(overrides: Partial<SpecificationEntity> = {}) {
  const raw = {
    id: overrides.id ?? `${DOMAIN_PREFIX}_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    kind: overrides.kind ?? "invariant",
    projectId: overrides.projectId ?? `prj_${ulid().toLowerCase()}`,
    requiredForGate: overrides.requiredForGate ?? faker.datatype.boolean(),
    specId: overrides.specId ?? `spc_${ulid().toLowerCase()}`,
    status: overrides.status ?? "draft",
    title: overrides.title ?? faker.lorem.sentence(),
    versionRef: overrides.versionRef ?? faker.lorem.word(),
    ...overrides,
  };

  return specificationsSchemas.Specification.parse(raw);
}

export function specificationBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createSpecification(overrides));
}
