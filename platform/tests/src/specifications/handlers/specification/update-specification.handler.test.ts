/**
 * UpdateSpecification handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: specifications
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { updateSpecification } from "@vericlause/api-server/domains/specifications/handlers";
import type { SpecificationsDDDDependencies } from "@vericlause/api-server/domains/specifications";
import { createSpecification as createSpecificationEntity } from "@vericlause/factories/specifications";
import { createUpdateSpecificationRequest } from "@vericlause/factories/specifications";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@vericlause/tests/shared";
import { SPECIFICATIONS_USE_CASES } from "../__factories__/use-cases.js";

describe("UpdateSpecification", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: SpecificationsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<SpecificationsDDDDependencies>(
      SPECIFICATIONS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with updated entity", async () => {
    const entity = createSpecificationEntity({ orgId });
    const input = createUpdateSpecificationRequest();
    const updatedEntity = { ...entity, ...input };
    const useCaseResult = { data: updatedEntity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.params = { specId: entity.id };
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeUpdateSpecification, "execute").mockResolvedValue(useCaseResult);

    await updateSpecification(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeUpdateSpecification.execute).toHaveBeenCalledWith(expect.objectContaining({ specId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
