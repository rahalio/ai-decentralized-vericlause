/**
 * GetContractProject handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: contract-projects
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getContractProject } from "@vericlause/api-server/domains/contract-projects/handlers";
import type { ContractProjectsDDDDependencies } from "@vericlause/api-server/domains/contract-projects";
import { createContractProject as createContractProjectEntity } from "@vericlause/factories/contract-projects";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@vericlause/tests/shared";
import { CONTRACTPROJECTS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetContractProject", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ContractProjectsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ContractProjectsDDDDependencies>(
      CONTRACTPROJECTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createContractProjectEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetContractProject, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { projectId: entity.id };

    await getContractProject(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetContractProject.execute).toHaveBeenCalledWith(expect.objectContaining({ projectId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { projectId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetContractProject, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getContractProject(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
